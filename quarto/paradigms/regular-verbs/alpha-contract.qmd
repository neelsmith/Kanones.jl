# Regular thematic (-ω) verbs, alpha contract: τιμάω


```{julia}
#| echo: false
#| output: false
#| warning: false
repo = dirname(pwd()) |> dirname |> dirname
using Kanones, CitableParserBuilder, Markdown
# 1. inflectional rules
lgr = joinpath(repo, "datasets", "literarygreek-rules")
# 2. core vocab
corevocab = joinpath(repo, "datasets", "corevocab")
ds = dataset([lgr, corevocab])
τιμαω = LexemeUrn("lsj.n103972")
```

```{julia}
verb_conjugation_md(τιμαω, ds) |> Markdown.parse
```
